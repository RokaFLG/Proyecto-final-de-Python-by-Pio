if (window.location.pathname === '/blog/chat/') {
    document.body.classList.replace('bg-background', 'bg-primary');
  } else {
    document.body.classList.replace('bg-primary', 'bg-background');
  }